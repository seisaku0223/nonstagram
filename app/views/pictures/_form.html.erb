<%= form_for(@picture) do |f| %>

  <%= f.label :pictuer %>
  <%= f.file_field :picture, class: 'form-control' %>
  <br>
  <%= f.label :comment %>
  <%= f.text_field :comment, class: 'form-control' %>
  <br>
  <%= f.submit '投稿', class: 'btn btn-primary btn-block' %>
<% end %>

<%= link_to "画像一覧に戻る", pictures_path %>
